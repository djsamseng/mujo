# Mujo

`$ conda create --name mujo python=3.8`
`$ conda activate mujo`
`$ pip install -r requirements.txt`


To call `env.render(w, h)` then:
```bash
$ ./run.sh
$ ./run.sh viewer
$ ./run.sh render
```